pydantic>=2.6
fastapi>=0.110
uvicorn[standard]>=0.29
httpx>=0.27
python-dotenv>=1.0
anthropic>=0.34
pytest>=8.0

# ── optional, wire when building the real modules ──────────────────────────
# Caller fan-out search:  exa-py  ·  tavily-python  ·  google-search-results (serper)
# Voice leg is configured via provider dashboards: Vapi / Retell / Bland / Twilio
# Shared state upgrade:   redis
